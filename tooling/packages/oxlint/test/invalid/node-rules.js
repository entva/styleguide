/* eslint-disable no-unused-vars, no-undef */

// expect: no-path-concat, prefer-template
const configPath = __dirname + '/config.json';

// expect: no-new-require, new-cap
const logger = new require('logger');
